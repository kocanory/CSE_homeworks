/* CLUB 테이블은 13주차에 사용했던 테이블이니 생성하지 않아도 됨.
단, 지난 시간 실습을 수행하지 않았으면 생성해야 함. */

CREATE TABLE CLUB (
	ID INT PRIMARY KEY,
	NAME VARCHAR(10) NOT NULL,
	CATEGORY VARCHAR(20) NOT NULL);

INSERT INTO CLUB VALUES (1, '홍길동', '등산');
INSERT INTO CLUB VALUES (2, '김영수', '프로그래밍');

/* 14주차 실습 테이블 */

CREATE TABLE DEPT (
	ID INT PRIMARY KEY,
	NAME VARCHAR(10) NOT NULL,
	BUILDING VARCHAR(20) NOT NULL);

INSERT INTO DEPT VALUES (10, "컴퓨터공학", "신공학관");

CREATE TABLE STUDENT (
	ID INT PRIMARY KEY,
	NAME VARCHAR(10) NOT NULL,
	ADDRESS VARCHAR(50) NOT NULL,
	DID INT NOT NULL,
	FOREIGN KEY(DID) REFERENCES DEPT(ID));

INSERT INTO STUDENT VALUES (1, "홍길동", "서울시 중구", 10);